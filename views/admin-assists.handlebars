<section class="section">
    <div class="container">
      <h1 class="title is-1">Assistance Management</h3>
      {{#unless assists}}
      <p>No assists yet.</p>
      {{/unless}}

      {{#if assists}}
      <p>Here is a list of all people we have assisted which are currently in the system.</p>
      
      {{#if unconfirmedAssists}}
      <h3 class="title is-3">Unconfirmed Assistance Requests</h3>
      <table>
        <tr>
          <th>ID</th>
          <th>Name</th>
          <th>Date Requested</th>
          <th>Confirmed?</th>
        </tr>
      {{#each unconfirmedAssists}}
        <tr>
          <td>{{this.assistId}}</td>
          <td>{{this.assistUserEmail}}</td>
          <td>{{this.createdAt}}</td>
          <td>
            <button class="confirm button is-success is-small" data-assistid="{{this.assistId}}">
            Confirm Requested
            </button>
          </td>

        </tr>
      {{/each}}
      </table>
      {{/if}}

      {{#if confirmedAssists}}
      <h3 class="title is-3">Confirmed Assistance Requests</h3>
      <table>
        <tr>
          <th>ID</th>
          <th>Name</th>
          <th>Date Requested</th>
          <th>Confirmed?</th>
        </tr>
      {{#each confirmedAssists}}
        <tr>
          <td>{{this.assistId}}</td>
          <td>{{this.assistUserEmail}}</td>
          <td>{{this.createdAt}}</td>
          <td>{{this.confirmed}}</td>
        </tr>
      {{/each}}
      </table>
      {{/if}}

      {{/if}}
    </div>
</section>

<script type="text/javascript" src="/js/admin-assists.js"></script>
